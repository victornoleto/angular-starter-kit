import { TableLoadingDirective } from './table-loading.directive';

describe('TableLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new TableLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
