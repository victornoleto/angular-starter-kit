import { InputPasswordDirective } from './input-password.directive';

describe('InputPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
